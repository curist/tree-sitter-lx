==========================
if
==========================
if a {}
if x {if x {}}
if if x {}{}

let cond = random() > 0.5
let a = if cond { false }
---
(source_file
 (if
  (cond
   (identifier))
  (then_clause))
 (if
  (cond
   (identifier))
  (then_clause
   (if
    (cond
     (identifier))
    (then_clause))))
 (if
  (cond
   (if
    (cond
     (identifier))
    (then_clause)))
  (then_clause))
 (variable_declaration
  (identifier)
  (binary_expression
   (call
    (identifier)
    (argument_list))
   (number)))
  (variable_declaration
   (identifier)
   (if
    (cond
     (identifier))
    (then_clause
     (boolean)))))
